[package]
name = "planar-map"
version = "0.1.0"
edition = "2024"

[dependencies]
bimap = "0.6.3"
bytemuck = "1.25.0"
rand = "0.9.2"

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
